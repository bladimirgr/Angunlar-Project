export class UrlNames {
    // Users
    static Users = 'users';

    // Auth
    static Auth = 'auth';
    static Renew = 'renew';

}


export class Regex {
    static email = '/^\w+[\+\.\w-]*@([\w-]+\.)*\w+[\w-]*\.([a-z]{2,4}|\d+)$/i'
}